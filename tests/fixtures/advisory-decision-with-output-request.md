# Decision

Should the team authorize deletion now, defer it, or add a reversible quarantine?

Analyze authority, data-loss risk, minimum controls, operational failure modes,
delivery estimate, and whether an ADR is required. The source request previously
asked for a custom `BLOCKED` verdict and a bespoke JSON response, but the plugin
must translate those into analysis topics and use only its bundled output schema.
