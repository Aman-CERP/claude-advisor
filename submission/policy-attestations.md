# Policy attestation record

## Verified from repository evidence

- The submission is skills-only and contains no MCP server or hosted app.
- Both skills require explicit invocation and set `allow_implicit_invocation: false`.
- The runner uses subprocess argument arrays and never enables a shell.
- Claude runs in safe mode with an empty tool set, Chrome disabled, session persistence disabled, and no permission-bypass flag.
- GitHub access is read-only and the plugin never comments, approves, labels, merges, commits, or pushes.
- The plugin sends no AmanERP telemetry and stores no credentials.
- Selected input is sent directly to Anthropic through the user's own Claude Code authentication.
- Public copy identifies unsupported hosted surfaces and the independent, non-affiliated publisher relationship.
- Automated tests require no private network, private repository, live credential, or AmanERP-only context.

## Human confirmations required immediately before portal submission

- Select the verified AmanERP business identity in the same OpenAI Platform organization and project used for the draft.
- Confirm the submitter has Apps Management write access.
- Confirm all four AmanERP public URLs are live and contain the reviewed release copy.
- Re-read the current OpenAI submission policies and attestations in the portal; do not rely on this repository as a substitute for the live form.
- Confirm every selected country currently supports the required OpenAI and Anthropic surfaces and is covered by AmanERP support and legal terms.
- Upload the final deterministic plugin ZIP and the production logo/screenshots from the same release commit.
- Confirm the neutral name and nominative dependency references remain acceptable under current trademark guidance; obtain counsel or Anthropic review if the public copy changes materially.
