# Approved context

- The new parser passes unit tests against the current fixture corpus.
- No production shadow comparison has been run.
- The deployment platform supports percentage-based feature flags and immediate rollback.
- The output schema is intended to remain unchanged, but compatibility has not been measured against retained production samples.
